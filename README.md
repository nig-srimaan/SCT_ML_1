# House Price Prediction

A machine learning model to predict house prices based on the Ames Housing dataset.

## Setup

1. Clone the repository.
2. Install the required dependencies:
   ```bash
   pip install -r requirements.txt
